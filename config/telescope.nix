{
  plugins.telescope = {
    enable = true;
    extensions = {
      file_browser.enable = true;
      frecency.enable = true;
      fzf-native.enable = true;
      # TODO: Re-enable when projects is fixed
      # project-nvim.enable = true;
      undo.enable = true;
    };
  };
}
